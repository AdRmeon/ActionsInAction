name: My First Workflow
on:
  push:
    branches: main
  workflow_dispatch:
jobs:
  MyFirstJob:
    environment: MyEnv
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "👋 Hello World!"
          echo "::debug::${{ env.MYVAR }}"
      - name: Checkout
        uses: actions/checkout@v4.2.2
      - name: List files in repository
        run: |
          echo "The repository ${{ github.repository }} contains the following files:" 
          tree
